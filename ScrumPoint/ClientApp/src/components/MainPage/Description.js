import React from 'react';

const Description = props => (
  <div>
        <h3 className="align-text-center">Description</h3>

        <p> Game design technique for estimating effort or relative size of development goals in software development.
            Members of the group make estimates by playing numbered, numbers are invisible. After, the numbers are revealed, and the estimates are then discussed. By hiding the figures in this way,
            the group can avoid the cognitive bias of anchoring, where the first number spoken aloud sets a precedent for subsequent estimates.</p>
  </div>
);

export default Description;
