import React from 'react';

const UserObserver = props => (
    <div className="row">
        <div className="user-name">
            <p>{props.name} observer</p>
        </div>
  </div>
);

export default UserObserver;
